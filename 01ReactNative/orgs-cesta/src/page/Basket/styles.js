import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerBasket: {
    paddingHorizontal: 8,
    paddingVertical: 16,
  },
    title: {
    color: '#464646',
    fontWeight: 'bold',
    marginTop: 32,
    fontSize: 20,
    marginBottom: 0,
    lineHeight: 32, 
  },
});

export default styles;